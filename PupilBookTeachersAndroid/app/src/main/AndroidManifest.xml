<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="src.pupilbookteachers" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" >
        </activity>
        <activity
            android:name=".StudentsActivity"
            android:label="@string/title_activity_clasification" >
        </activity>
        <activity
            android:name=".ClasificationActivity"
            android:label="@string/title_activity_clasification" >
        </activity>
        <activity
            android:name=".ResultsActivity"
            android:label="@string/title_activity_results" >
        </activity>
        <activity
            android:name=".ResultsListActivity"
            android:label="@string/title_activity_results_list" >
        </activity>
        <activity
            android:name=".SheduleTabsActivity"
            android:label="@string/title_activity_shedule_tabs" >
        </activity>
        <activity
            android:name=".SheduleDayActivity"
            android:label="@string/title_activity_shedule_day" >
        </activity>
    </application>

</manifest>
